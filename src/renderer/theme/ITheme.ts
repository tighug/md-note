export default interface ITheme {
  color: {
    primary: string;
    secondary: string;
    normal: string;
    bg: {
      primary: string;
      secondary: string;
      disabled: string;
    };
    text: {
      primary: string;
      secondary: string;
      disabled: string;
    };
  };
}
